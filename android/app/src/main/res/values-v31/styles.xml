<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Android 12+ (API 31+) SplashScreen API 适配
        使用 Theme.Material.Light.NoActionBar 作为父主题以支持 postSplashScreenTheme
    -->
    <style name="LaunchTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/launch_background</item>
        <item name="android:postSplashScreenTheme">@style/NormalTheme</item>
    </style>

    <!-- 必须保留 NormalTheme 定义，供 manifest 中 meta-data 引用 -->
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>