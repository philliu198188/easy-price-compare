name: easy_price
description: 易比价 - Flutter 比价 App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  shimmer: ^3.0.0
  shared_preferences: ^2.2.2
  http: ^1.1.0
  cupertino_icons: ^1.0.6
  go_router: ^13.0.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  test: ^1.24.0

flutter:
  uses-material-design: true